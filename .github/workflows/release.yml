name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/void-linux/void-glibc-full:latest
      options: --privileged -v /dev:/dev
    steps:
      - uses: actions/checkout@v4
      - name: Install build dependencies
        run: |
          xbps-install -Syu xbps
          xbps-install -Sy xtools-minimal dosfstools e2fsprogs util-linux openssl
      - name: Build
        working-directory: server
        run: ./build_img.sh
      - uses: actions/upload-artifact@v4
        with:
          name: avoid-img
          path: server/avoid.img

  package-gz:
    needs: build
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/void-linux/void-glibc-full:latest
      options: --privileged -v /dev:/dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: avoid-img
          path: server
      - name: Compress
        working-directory: server
        run: ./build_img_gz.sh avoid.img
      - uses: actions/upload-artifact@v4
        with:
          name: avoid-img-gz
          path: server/avoid.img.gz

  package-qcow2:
    needs: build
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/void-linux/void-glibc-full:latest
      options: --privileged -v /dev:/dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: avoid-img
          path: server
      - name: Install dependencies
        run: |
          xbps-install -Syu xbps
          xbps-install -Sy qemu-img e2fsprogs util-linux
      - name: Convert to qcow2
        working-directory: server
        run: ./build_qcow2.sh avoid.img
      - uses: actions/upload-artifact@v4
        with:
          name: avoid-qcow2
          path: server/avoid.qcow2

  release:
    needs: [package-gz, package-qcow2]
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/void-linux/void-glibc-full:latest
      options: --privileged -v /dev:/dev
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: avoid-img-gz
          path: .
      - uses: actions/download-artifact@v4
        with:
          name: avoid-qcow2
          path: .
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            avoid.img.gz
            avoid.qcow2
